{
  "name": "openshift-reference-springboot-server",
  "version": "0.0.1",
  "config": { "baseURL": "http://localhost:8080/"},
  "scripts": {
    "test": "npm-run-all fetchCounter",
    "version": "newman --version",
    "hentUtkast": "newman run -e systemtest/postman/environment.json systemtest/postman/fetchCounter.json"
  },
  "dependencies": {
    "newman": "3.6.0",
    "npm-run-all": "^4.0.2"
  }
}
